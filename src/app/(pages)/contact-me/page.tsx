import React from "react";
import ConnectPage from "@/components/ConnectPage/Connect";
export const metadata = {
  title: "Contact Newton — Get In Touch",
  description:
    "Reach out to Newton for freelance opportunities, collaborations, and new projects.",
};
const page = () => {
  return <ConnectPage />;
};

export default page;
